# RiskRadar Configuration
APP_ENV=development
LOG_LEVEL=INFO

# Google APIs / Services (if exporting to Google Sheets / Slides)
GOOGLE_APPLICATION_CREDENTIALS=credentials.json

# Optional external LLM / Notification Webhooks
SLACK_WEBHOOK_URL=
ALERT_EMAIL_RECIPIENT=
